# Задача: preflight InfoApi для Авто-Рыбалки + fix `hbar`

## Контекст

- [x] Разобрать новые логи `Logs/*.*` с учётом поддиректорий.
- [x] Подтвердить, что часть циклов рыбалки уходит в `act=2` без свежей пред-проверки рук.
- [x] Подтвердить, что порог усталости может проскочить между `NeverTimer` и новым стартом цикла.
- [x] Проверить текущий контур `hbar` в `map.js` / `hp.js` / `HtmlUtils`.

## План правки

- [x] Вынести результат preflight InfoApi в общий state для `MainPhp` и `FishAjaxPhp`.
- [x] Запускать preflight до старта нового fish-cycle, не добавляя новый сетевой контур.
- [x] При необходимости маршрутизировать цикл обратно в `main.php`, чтобы существующие ветки выпили Блаж/переодели снасть.
- [x] Исправить выбор реального DOM-узла `hbar`, если в документе есть скрытый stub.
- [x] Добавить `curTier` в HTML строки HP/MA возле ника.
- [ ] Собрать `:app:compileDebugJavaWithJavac`.

## Риски

- Не создать дублирующий recovery-контур поверх `AutoFishCheckUd/AutoFishWearUd`.
- Не сломать существующий `HtmlUtils.getJsFix()`, который обязан оставлять stub `hbar`.
- Не привнести mojibake: все изменения сохранить в UTF-8 without BOM.
