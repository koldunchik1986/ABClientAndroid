# Задача: завершение боя без капчи (кнопка `FEND`)

Дата: 2026-03-05
Статус: `[x]` Выполнено

## План

- [x] Проанализировать `Logs/logcat_runtime_20260305_02.txt` и подтвердить цикл незавершения.
- [x] Сверить текущую Android-реализацию `MainPhp.mainPhpFight(...)` с C#-эталоном.
- [x] Добавить fallback-разбор HTML-формы завершения боя (`form[name=FEND]`) без обхода капчи.
- [x] Реализовать автосабмит `FEND`, если `code` не требуется.
- [x] Остановить бесконечный `main.php`-reload при `FightLink` пустом и отсутствии корректного submit.
- [x] Проверить сборку `:app:compileDebugJavaWithJavac`.
- [x] Обновить статус задачи.

## Ограничение по безопасности

- [x] Не реализовывать обход/эмуляцию обхода CAPTCHA.
- [x] Поддерживать только штатный сценарий сервера:
  - без капчи: автосабмит формы завершения;
  - с капчей: ручной ввод через существующий поток.

## Результат

- В `MainPhp` добавлен fallback-разбор формы `FEND` и генерация авто-submit страницы только для сценария без капчи.
- Если сервер требует `code` и значение пустое, автосабмит отключается (капча не обходится).
- Убран fallback-редирект `main.php` при пустом `FightLink`, который создавал цикл; вместо этого:
  - при наличии формы — выполняется submit,
  - иначе оставляется исходный HTML кадра боя.
- Сборка `:app:compileDebugJavaWithJavac` успешна.
