# Задача: пароль не запоминается после входа (follow-up)

Дата: 2026-03-24  
Статус: `[x]` Выполнено

## План

- [x] Проверить `Logs/logcat_runtime_20260324_12.txt` по сценарию входа с галочкой.
- [x] Сверить логику выбора профиля/чекбокса/сохранения пароля в `LoginActivity`.
- [x] Исключить сценарий сохранения в «чужой» файл профиля (по `UserNick` вместо `id`).
- [x] Добавить стабильное восстановление именно последнего использованного `profile.id`.
- [x] Уточнить UX-текст чекбокса (`Запомнить пароль`).
- [x] Проверить сборку.

## Итог

- На экране логина приоритетно восстанавливается последний `profile.id` из локального состояния.
- Флаг чекбокса теперь работает как явное `remember password` (`UserAutoLogon`) и не теряется между кликом и async-авторизацией.
- Сохранение/удаление профиля переведено на `id` (если он есть), что предотвращает запись в другой файл при несовпадении имени файла и ника.
- Строка чекбокса изменена на `Запомнить пароль`.
- Сборка `:app:compileDebugJavaWithJavac` успешна.
