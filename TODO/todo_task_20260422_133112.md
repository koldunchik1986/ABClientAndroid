# План и выполнение задач (2026-04-22 13:31:12)

## Контекст задания

- [x] Подтверждено правило: неизвестные новые имена добавляются как обычный тип `Бот`.
- [x] Цель: убрать hardcode типов противников, перенести классификацию в `bottypes.xml`, сохранить автодобавление новых имён.

## План реализации

- [x] Исправить `BossAuto`: динамический timeout возврата на исходную клетку.
- [x] Исправить `BossAuto`: отправка clan war-deny сообщения через `sendClanBossWarDeniedMessageIfNeeded(...)`.
- [x] Исправить stale-состояние боевой капчи в `MainActivity` и `AutoModeForegroundService`.
- [x] Реализовать `bottypes.xml` как canonical-источник в `assets/info`.
- [x] Перевести `LezBotsClassCollection` на runtime XML (`files/info/bottypes.xml`) с bootstrap из assets.
- [x] Реализовать auto-upsert неизвестных имён в runtime XML с типом `bot`.
- [x] Подключить data-driven boss-check в `LezFight` и `UnderAttackManager`.
- [x] Инициализировать новый контур при старте приложения (`ABClientApplication`).
- [x] Прогнать компиляцию `:app:compileDebugJavaWithJavac`.
- [x] Проверить diff на mojibake-паттерны.

## Примечания

- [x] Папка `ABClient/` не изменялась (read-only).
- [x] Новые обращения к `AppVars.VCode` не добавлялись.

## Дополнение 2026-04-22 (авто-лечение: wars/fight guard)

- [x] Проанализировать текущую точку принятия решения авто-лечения в `RoomManager` (`selectRoomAutoCureTarget` / `buildAutoCureTarget`).
- [x] Добавить ограничение для friend/neutral: не запускать лечение, если наш клан участвует в текущих клан-войнах.
- [x] Добавить ограничение для friend/neutral: не запускать лечение, если клан цели участвует в текущих клан-войнах.
- [x] Добавить ограничение для friend/neutral: не запускать лечение, если у цели активный `fightFid`.
- [x] Обеспечить повторную проверку после следующих room-обновлений: guard-состояние кэшируется кратко, затем перечитывается из `info_api`.
- [x] Прогнать компиляцию `:app:compileDebugJavaWithJavac` после правок.

## Дополнение 2026-04-22 (уведомления + комментарии по фиксам)

- [x] Добавить dedup/throttle-уведомления в чат по причинам пропуска авто-лечения (`target_in_fight`, `current_wars`, `self_clan_wars`).
- [x] Подробно прокомментировать новый guard-контур авто-лечения в `RoomManager` (зависимости, переменные, источники данных).
- [x] Подробно прокомментировать ранее внесённые фиксы в `BossAuto` (dynamic return timeout + wars deny).
- [x] Подробно прокомментировать ранее внесённые фиксы stale-captcha в `MainActivity` и `AutoModeForegroundService`.
- [x] Подробно прокомментировать ранее внесённые data-driven фиксы bottypes в `LezBotsClassCollection`, `LezFight`, `UnderAttackManager`, `ABClientApplication`.
