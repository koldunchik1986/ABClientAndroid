# План и выполнение задач (2026-04-22 13:31:12)

## Контекст задания

- [x] Подтверждено правило: неизвестные новые имена добавляются как обычный тип `Бот`.
- [x] Цель: убрать hardcode типов противников, перенести классификацию в `bottypes.xml`, сохранить автодобавление новых имён.

## План реализации

- [x] Исправить `BossAuto`: динамический timeout возврата на исходную клетку.
- [x] Исправить `BossAuto`: отправка clan war-deny сообщения через `sendClanBossWarDeniedMessageIfNeeded(...)`.
- [x] Исправить stale-состояние боевой капчи в `MainActivity` и `AutoModeForegroundService`.
- [x] Реализовать `bottypes.xml` как canonical-источник в `assets/info`.
- [x] Перевести `LezBotsClassCollection` на runtime XML (`files/info/bottypes.xml`) с bootstrap из assets.
- [x] Реализовать auto-upsert неизвестных имён в runtime XML с типом `bot`.
- [x] Подключить data-driven boss-check в `LezFight` и `UnderAttackManager`.
- [x] Инициализировать новый контур при старте приложения (`ABClientApplication`).
- [x] Прогнать компиляцию `:app:compileDebugJavaWithJavac`.
- [x] Проверить diff на mojibake-паттерны.

## Примечания

- [x] Папка `ABClient/` не изменялась (read-only).
- [x] Новые обращения к `AppVars.VCode` не добавлялись.
