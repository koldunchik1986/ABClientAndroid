# Задача: зависание после завершения боя (десинхрон AutoBoi)

## Контекст
- Лог для анализа: `Logs/logcat_runtime_20260303_12.txt`.
- Симптом: бой завершён, но не нажимается `Завершить бой`, персонаж «висит».

## План
- [x] Проверить лог и подтвердить причину зависания.
- [x] Исправить ветку завершения боя в Android-коде.
- [x] Выполнить сборку debug и проверить компиляцию.
- [x] Зафиксировать результат в TODO.

## Что выявлено
- [x] В логе подтверждён десинхрон состояния:
  - persisted авто-бой активен (`autoFight=true`),
  - runtime состояние `AppVars.Autoboi=AutoboiOff`.
- [x] Из-за этого `MainPhp.mainPhpFight(...)` не входил в авто-ветку завершения боя (`AutoboiOn`), и бой не завершался.

## Что изменено
- [x] `app/src/main/java/ru/neverlands/abclient/postfilter/MainPhp.java`
  - добавлен self-heal десинхрона состояния авто-боя:
    - если профиль/настройка авто-боя включены, но `AppVars.Autoboi=AutoboiOff`,
    - и при этом не активен captcha-flow,
    - runtime состояние восстанавливается в `AutoboiOn`.
  - добавлено чтение persisted флага авто-боя через `AutoFunctionsManager`.
  - URL капчи завершения боя теперь вычисляется один раз на цикл `fightEnded` и переиспользуется в auto/manual ветках.

## Проверка
- [x] Сборка: `.\gradlew.bat assembleDebug`
- [x] Результат: `BUILD SUCCESSFUL`.
