# Задача: Защита переименования клеток карты вне природы

## Контекст
- Баг: при входе в город ch.php?lo=1 отдаёт placename городских комнат (например, Рынок), и это ошибочно перезаписывает последнюю клетку природы (egNum карты).

## План
- [x] Найти точку синхронизации названий (RoomManager.syncCellNameFromRoomHtml).
- [x] Добавить guard: синхронизировать только если открыт контур карты природы.
- [x] При выходе из контура карты очищать pending-данные синхронизации.
- [x] Собрать проект и проверить отсутствие регрессий компиляции.

## Реализация
- Добавлен метод isNatureMapContextForCellRename():
  - проверяет, что Profile.MapLocation указывает на известную клетку ExtMap.Cells;
  - подтверждает картографический контекст по url_main_top (get_id=56&act=10&go=ret) или по map-пейлоаду ar map = [[ в ContentMainPhp.
- В syncCellNameFromRoomHtml(...) добавлен ранний выход при не-картографическом контексте с очисткой pending.

## Проверка
- [x] ./gradlew :app:compileDebugJavaWithJavac — успешно.