# Задача: портирование «Авто-Боссы» (Android, 1:1 по текущей логике проекта)

## Контекст
- Цель: добавить новую авто-функцию `Авто-Боссы` как отдельный модуль `BossAuto.java`, без дублирования уже существующей логики.
- Ограничения:
  - использовать текущие контуры `Auto-Компас`, `FastAction`, `Авто-Бой`, `Авто-Лечение`;
  - не раздувать код дублирующими обработчиками;
  - не менять `ABClient/*`.

## Источники текущей реализации (Android)
- `AutoFunctionsManager` — единая точка флагов авто-функций и запуска/остановки.
- `CompasAuto` — поиск цели по `pinfo` и обход клеток.
- `RoomManager` — актуальный список игроков клетки (`ch.php?lo=1`) и room-update hook.
- `ChatFilter`/`Chat` — поток входящих сообщений и отправка в чат.
- `FastActionManager.fastAttackZas(nick)` — быстрый свиток защиты.

## Технический план
- [x] Добавить `BossAuto.java` (состояние сценария, парсинг событий/ответов, оркестрация).
- [x] В `AutoFunctionsManager` добавить API-обертки для `Auto-Боссы` и интеграцию с существующими хуками.
- [x] Подключить входящие сообщения чата к `BossAuto` через `ChatFilter`.
- [x] Подключить room-update к `BossAuto` через `AutoFunctionsManager.onRoomUsersUpdated(...)`.
- [x] Подключить `tickAutoBoss()` в `AutoModeForegroundService`.
- [x] Добавить `QuickActionType.AUTO_BOSS` и поддержку в `QuickButtonsPanel`/`FunctionListAdapter`.
- [x] Добавить окно настроек `Авто-Боссы` (long-press в `QuickButtonsPanel`) с сохранением параметров в `BossAuto`.
- [x] Реализовать pause/resume авто-функций (кроме `Авто-Бой`, `Авто-Лечение`, `Авто-Боссы`) с возвратом на исходную клетку после боя.
- [x] Обновить `AGENTS.MD` правилом по `Авто-Боссы`.
- [x] Прогнать `:app:compileDebugJavaWithJavac`.

## Сценарий приемки
- [ ] При включенном `Авто-Боссы` и сообщении:
      `Внимание! Случайное событие! Монстр "..." напал на игрока Nickname.`
      извлекается `Nickname` и стартует поиск через `Авто-Компас`.
- [ ] В чат уходит `%<Nickname> Подскажи на какой клетке Босс?`.
- [ ] При ответе `Nickname` с клеткой `XX-XXX` маршрут перестраивается на указанную клетку.
- [ ] После нахождения цели выполняется `FastAction` свиток защиты на `Nickname`.
- [ ] После боя персонаж возвращается на исходную клетку, затем восстанавливаются паузенные авто-функции.

## Обновление 2026-03-28: БД режим + fid-контроль + fallback по логу боя
- [x] Добавлен БД режим в настройки `Авто-Боссы` (UI + `AutoFunctionsManager` + `BossAuto`).
- [x] В `NeverApi.PinfoCompassSnapshot` добавлены поля `clanToken`, `fightFid`, `level` с парсингом `parameters[2]/[7]/[1]`.
- [x] На старте сценария `Авто-Босс` выполняется проверка БД-режима:
  - [x] разный клан при включенном БД режиме → отмена с сообщением;
  - [x] отсутствует наш клан → продолжаем поиск без учета БД режима с сообщением.
- [x] Добавлен контроль `fid` во время поиска цели:
  - [x] 1 тик grace при пропаже `fid`;
  - [x] отмена на 2-м тике с сообщением «цель уже не в бою».
- [x] В сообщениях старта добавлено кликабельное слово `бою` (`logs.fcg?fid=...`).
- [x] Перед кастом «Свиток Защиты» добавлен fallback-парсинг лога боя:
  - [x] если исходная цель жива — каст на нее;
  - [x] если исходная цель мертва — каст на первого живого союзника;
  - [x] если живых союзников нет — отмена сценария с причиной.
- [x] Проверка компиляции: `:app:compileDebugJavaWithJavac` (успешно).

## Обновление 2026-03-28: модуль «Кланы» + wars.cgi в БД-режиме Auto-Босс
- [x] Добавлен единый менеджер `ClanWarsManager`:
  - [x] sync `clans.txt` через общий слой (переиспользуется из `ContactsActivity` и `ClansActivity`);
  - [x] sync `wars.cgi` + in-memory/file cache (`files/info/wars_cache.txt`);
  - [x] API `isClanTokenInCurrentWars(...)` для проверки БД-режима.
- [x] Добавлен экран `ClansActivity` с вкладками:
  - [x] «Кланы» (кнопка «Синхронизировать состояние кланов»);
  - [x] «Текущие войны» (табличный вывод, формат дат `dd.MM.yyyy HH:mm:ss`, зона `Europe/Kiev`).
- [x] Добавлен пункт бокового меню `Кланы` и переход из `MainActivity`.
- [x] Добавлен post-login sync `wars.cgi` в `AutoFunctionsManager` (мягко, без блокировки логина).
- [x] Расширен БД-режим `BossAuto`:
  - [x] если `targetClanToken` входит в wars-cache — сценарий отменяется с сообщением в чат.
