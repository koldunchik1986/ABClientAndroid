# Задача: отладка пропуска событий Босса в чате

## Контекст
- Проверка пропуска системных сообщений вида: `Внимание! Случайное событие! Монстр "..." напал на игрока ...`
- Симптом: `Авто-Босс` не стартует, хотя внешние источники (бот) показывают, что событие было.

## Выполнено
- [x] Проанализированы runtime-логи `Logs/logcat_runtime_20260329_*.txt`:
  - [x] В исследованных интервалах отсутствуют строки `AUTO_BOSS_TRACE` о старте сценария.
  - [x] В ответах `ch.php?show=1&fyo=0` в большинстве тиков приходит короткий ответ (`Raw bytes: 38`) без новых сообщений.
- [x] В `BossAuto` расширен парсер события Босса:
  - [x] Добавлен более гибкий regex для ника цели (поддержка спецсимволов).
  - [x] Добавлена нормализация хвостовой пунктуации у ника.
  - [x] Добавлен debug-лог `parse miss boss-event`, если текст похож на событие, но не распарсился.
- [x] В `WebViewRequestInterceptor` добавлена диагностика `AUTO_BOSS_TRACE`:
  - [x] Логируется факт наличия boss-события прямо в `ch_refr` ответе (`show=1`), если оно действительно пришло.

## Проверка после фикса
- [ ] Запустить новый лог в момент ожидания события Босса.
- [ ] Убедиться, что при получении события есть лог:
  - `AUTO_BOSS_TRACE ch_refr boss-event: ...`
- [ ] Убедиться, что после этого появляется старт сценария `Авто-Босс`.
