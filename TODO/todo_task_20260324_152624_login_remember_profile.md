# Задача: восстановление последнего профиля и пароля при старте

Дата: 2026-03-24  
Статус: `[x]` Выполнено

## План

- [x] Проверить `Logs/logcat_runtime_20260324_11.txt` по цепочке старта `LoginActivity`.
- [x] Сопоставить логи с текущей логикой выбора профиля и восстановления пароля.
- [x] Проверить соответствие ПК-формату профиля (`user@lastlogon`, `user@autologon`) в Android `UserConfig`.
- [x] Исправить выбор профиля в UI для `AutoCompleteTextView` (вместо индексного `setSelection`).
- [x] Добавить обновление `LastLogin` при успешном входе.
- [x] Сохранить/читать `lastlogon` в XML профиля + fallback для старых профилей.
- [x] Проверить сборку `:app:compileDebugJavaWithJavac`.

## Итог

- На старте профиль теперь выбирается корректно для `ExposedDropdownMenu` (`setText(...)`), а не через курсорный `setSelection(...)`.
- Поле пароля и чекбокс `Запомнить меня` синхронизируются из выбранного профиля централизованно через `applySelectedProfile(...)`.
- После успешной авторизации профиль получает актуальный `LastLogin` (в .NET ticks), который сохраняется в `user@lastlogon`.
- Для старых `.profile` без `lastlogon` добавлен fallback на `file.lastModified()`, что исключает случайный выбор "первого файла".
- Сборка прошла успешно.
