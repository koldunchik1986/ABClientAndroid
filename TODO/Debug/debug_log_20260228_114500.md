# Журнал отладки 2026-02-28 11:45 — чат, отправка и системные сообщения

1) **Действие:** анализ `Logs/logcat_runtime_20260227_27.txt`.  
   **Обоснование:** проверить, отправляется ли сообщение и идут ли ответы `add_msg`.  
   **Результат:** есть `ch/but.php` и периодический `ch.php?show=1`, но нет `chatSubmit` и нет `add_msg` в ответах.  
   **Выводы:** JS‑хук формы не срабатывает или не на той форме.

2) **Действие:** усилен перехват формы в `ButPhp`.  
   **Обоснование:** форма может быть без имени `FBT`, нужно искать по наличию поля `text`.  
   **Результат:** перехватываем все формы, фильтруем по наличию `text/msg`, логируем `ABCLIENT_CHAT_SUBMIT`, делаем fallback сериализации.  
   **Выводы:** после сборки должны появиться логи `ABCLIENT_CHAT_SUBMIT` и `chatSubmit`.

3) **Действие:** добавлен глобальный `top.clr_input` → `AndroidBridge.chatClearInput`.  
   **Обоснование:** в логах ошибки `top.clr_input is not a function`, это ломает обработчик отправки.  
   **Результат:** `top.clr_input` не падает и очищает поле ввода.  
   **Выводы:** отправка не должна прерываться JS‑ошибкой.
