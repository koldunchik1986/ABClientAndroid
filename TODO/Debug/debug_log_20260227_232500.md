# Журнал отладки 2026-02-27 23:25 — чат не отправляет сообщения

1) **Действие:** анализ `Logs/logcat_runtime_20260227_24.txt`.  
   **Обоснование:** выявить причину, почему сообщения не появляются в чате.  
   **Результат:** есть регулярные запросы `ch.php?show=1&fyo=0`, но JS-ошибка `top.set_lmid is not a function`.  
   **Выводы:** ответ чата не может обновить `lmid`, скрипт падает; требуется глобальный `top.set_lmid`.

2) **Действие:** добавлены JS-мосты в `HtmlUtils.getJsFix`.  
   **Обоснование:** прокинуть `top.set_lmid` и `top.add_msg` в AndroidBridge.  
   **Результат:** `top.ch_refresh_n` теперь вызывает `AndroidBridge.chatRefreshN`, добавлены `top.set_lmid` и `top.add_msg`.  
   **Выводы:** обновление чата и лмид должны работать, после сборки ждём сообщений.
