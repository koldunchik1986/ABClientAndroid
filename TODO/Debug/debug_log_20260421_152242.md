# Debug Log — 2026-04-21 15:22:42

## Контекст
- Цепочка: авто-бой / завершение боя (`get_id=61&act=7`).
- Цель сессии: продолжить верификацию фикса anti-loop для повторного перехода по одному и тому же finish-link.

## Хронология действий
1. **Действие:** перечитал правила проекта (`.cursor/rules/AI.txt`, `.cursor/rules/general rule.txt`, `.cursor/rules/UTF8.ps1`, `AGENTS.MD`).
   - **Обоснование:** обязательный pre-check перед анализом/правками/ответом.
   - **Результат:** контекст ограничений подтверждён.

2. **Действие:** проверил текущее состояние git (`git status --short`, `git diff -- MainActivity.java`).
   - **Обоснование:** подтвердить состав незакоммиченных изменений по текущему фиксу.
   - **Результат:** изменён только `app/src/main/java/ru/neverlands/abclient/MainActivity.java`; anti-loop guard присутствует в ветке pending finish-link.

3. **Действие:** проверил корректность сборки (`./gradlew :app:assembleDebug`).
   - **Обоснование:** убедиться, что текущая правка не ломает проект.
   - **Результат:** `BUILD SUCCESSFUL`.

4. **Действие:** провёл статическую проверку целевого файла на прямой `Log.*` и на критичные артефакты.
   - **Обоснование:** соблюдение требований `AppLog` и anti-regression.
   - **Результат:** в `MainActivity.java` не обнаружен `import android.util.Log` и вызовы `Log.d/i/w/e`.

## Выводы
- Текущий anti-loop guard скомпилирован и локально валиден по статике.
- Для финального подтверждения нужен runtime прогон на устройстве с новыми логами.

## Следующий шаг
- [ ] Снять логи после установки нового debug APK и проверить:
  - отсутствие длинной серии одинаковых `navigating to pending finish link`;
  - появление новых anti-loop маркеров (`suppress duplicate pending finish navigation` / `repeated same pending finish link too many times`);
  - быструю сходимость в `main.php` без зависания цикла.

## Дополнение 2026-04-21 (captcha + postfilter crash)
5. **Действие:** повторно перечитал правила проекта (`.cursor/rules/AI.txt`, `.cursor/rules/general rule.txt`, `.cursor/rules/UTF8.ps1`, `AGENTS.MD`).
   - **Обоснование:** обязательный pre-check перед продолжением отладки и правок.
   - **Результат:** подтверждены ограничения (правки только в `app/`, без дублирующих контуров).

6. **Действие:** перепроверил кодовые decision points (`FightAuto.publishFightResultFromLogsIfNeeded`, `WebViewRequestInterceptor` captcha-guard).
   - **Обоснование:** локализовать причины из свежих логов (`PatternSyntaxException` + блокировка foreign challenge без bytes).
   - **Результат:** подтверждён аварийный regex в `FightAuto` и жёсткая блокировка `code.php` при отсутствии ожидаемых bytes.

7. **Действие:** внес правки в `FightAuto.java`.
   - **Обоснование:** устранить падение postfilter-цепочки на `act=7`.
   - **Результат:**
     - добавлен безопасный парсер `extractWinnerNickFromLogs(...)` без аварийного regex;
     - `publishFightResultFromLogsIfNeeded(...)` переведён на новый парсер winner.

8. **Действие:** внес правку в `WebViewRequestInterceptor.java` (captcha guard).
   - **Обоснование:** исключить сценарий, когда при открытом popup блокируется новый challenge (`code.php`) и клиент застревает на несинхронной картинке.
   - **Результат:** при mismatch challenge и отсутствии ожидаемых bytes guard больше не возвращает пустой ответ; запрос разрешается для resync, с предупреждающим логом.

9. **Действие:** выполнил компиляционную проверку (`./gradlew :app:compileDebugJavaWithJavac`).
   - **Обоснование:** подтвердить, что изменения в критичных цепочках не ломают сборку.
   - **Результат:** `BUILD SUCCESSFUL`.

10. **Действие:** проверил кодировку изменённых файлов (UTF-8 без BOM) и anti-mojibake grep.
   - **Обоснование:** соблюдение обязательных требований AGENTS по кодировке.
   - **Результат:** BOM не обнаружен; mojibake-паттерны в изменённых файлах отсутствуют.

## Актуальный статус
- [x] Исправлен crash `PatternSyntaxException` в postfilter-ветке `act=7`.
- [x] Смягчён captcha-guard при `expected bytes missing` (resync вместо hard-block).
- [ ] Собрать APK и проверить на устройстве новыми логами, что:
  - нет `Intercept failed` из `publishFightResultFromLogsIfNeeded`;
  - нет зависания на повторных `act=7` переходах;
  - после ввода корректной капчи бой завершается без ручного отключения авто-боя.
