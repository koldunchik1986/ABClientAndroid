# Debug log 2026-02-28 13:05

1. **Действие:** Прочитан лог `Logs/logcat_runtime_20260227_30.txt` через фильтр по чат‑меткам.  
   **Обоснование:** Уточнить, отрабатывает ли отправка и есть ли JS ошибки.  
   **Результат:** Есть `ABCLIENT_CHAT_SUBMIT`, но ответ `ch.php` падает на `top.clr_input`, а в `ch/but.php` ошибки `top.clan_private/ch_refresh/clr_chat`.  
   **Выводы:** POST‑ответ `ch.php` не проходит через инжекцию JS, поэтому стубы не объявлены; отправка требует ручной обработки ответа.

2. **Действие:** Проанализирован `WebAppInterface.chatSubmit`, `WebViewRequestInterceptor` (GET‑only).  
   **Обоснование:** Определить, почему POST не получает инжекцию JS.  
   **Результат:** `shouldInterceptRequest` не перехватывает POST, значит `HtmlUtils.injectJsFix` не применяется.  
   **Выводы:** Нужен ручной POST с разбором `add_msg/set_lmid`.

3. **Действие:** Реализован ручной POST в `WebAppInterface.chatSubmit`, парсинг `add_msg/set_lmid`.  
   **Обоснование:** Отправка сообщений должна работать независимо от WebView‑POST.  
   **Результат:** Добавлен HTTP POST с cookies, разбор ответа и обновление чата/`lmid`.  
   **Выводы:** Отправка должна работать без JS ошибок `top.clr_input`.

4. **Действие:** Добавлены обработчики `chatClearChat`, `chatRefreshNow`, `chatClanPrivate` и `MainActivity.requestChatRefreshNow`.  
   **Обоснование:** Кнопки чата вызывают `top.clr_chat/top.ch_refresh/top.clan_private`.  
   **Результат:** Эти действия теперь реализованы через AndroidBridge.  
   **Выводы:** Ошибки в `ch/but.php` должны исчезнуть после сборки.

5. **Действие:** Обновлён план в `TODO/todo_chat.md`.  
   **Обоснование:** Зафиксировать выполненный этап отправки сообщений.  
   **Результат:** Шаг «связка с вводом» помечен как выполненный.  
   **Выводы:** Можно переходить к проверке системных сообщений/ChatFilter.

6. **Действие:** Исправлена кодировка POST для чата и добавлены обработчики `change_chatsetup/chatspeed/latrus`.  
   **Обоснование:** Кириллица уходит «иероглифами», а кнопки режима/скорости/транслита падают на `top.*`.  
   **Результат:** Перекодирование URL‑payload UTF‑8 → windows‑1251; добавлены JS‑стабы и нативные обработчики для кнопок.  
   **Выводы:** Отправка кириллицы и кнопки чата должны работать корректно.

7. **Действие:** Исправлен клик по нику в клан‑сообщениях и добавлены маркеры для `%clan%/%pair%`.  
   **Обоснование:** В клан‑чате клик вставлял обычный `<Nick>` вместо `%clan%%<Nick>%`.  
   **Результат:** При получении `clan:`/`pair:` добавляются дополнительные `%` в `alt/title`; `to_what_who` теперь читает `alt/title`; `chatSayPrivate` добавляет `%clan%/%pair%`.  
   **Выводы:** Клик по нику в клан‑сообщении должен формировать правильный префикс.

8. **Действие:** Доработан `to_what_who` для клан/парных сообщений.  
   **Обоснование:** У некоторых клан‑сообщений нет `%%` в `alt`, поэтому префикс не ставился.  
   **Результат:** Определение `clan:`/`pair:` по тексту строки, и передача в `say_private` с `%`/`%%`.  
   **Выводы:** Клик по нику в клан‑сообщении должен вставлять `%clan%%<Nick>%` даже без `alt`.
