# Debug Log — 2026-03-05 10:16:00

## Цель
Понять по `Logs/logcat_runtime_20260305_02.txt`, почему бой не завершается.

## Хронология действий

1. **Действие:** просмотр ключевых меток в логе (`MainPhp`, `LezFight`, `AutoModeFgService`, `onPageFinished`, `FightLink`).
   - **Обоснование:** восстановить фактическую последовательность после конца боя.
   - **Результат:** обнаружен повторяющийся цикл:
     - `mainPhpFight: FIGHT ENDED with autoboi ON - processing finish`
     - `mainPhpFight: FightLink missing, redirecting to main.php`
     - `WebAppInterface: redirectToUrl: http://neverlands.ru/main.php`
     - `onPageFinished: POST-ответ... перезагружаем http://neverlands.ru/main.php?r=...`
     - затем снова приходит `fight_ty`.

2. **Действие:** проверка `fight_ty` в проблемном кадре.
   - **Обоснование:** понять, какой именно серверный state приходит после "конца боя".
   - **Результат:** стабильно приходит `fight_ty = [1,300,50,0,6,...,"2","720865496",...,4]`.
     - `fight_ty[3]=0` (не активный удар),
     - `fight_ty[4]=6` (состояние вне активного хода),
     - `LogBoi` не меняется (`720865496`), но challenge/hash в `fight_ty[5]` меняется между итерациями.

3. **Действие:** сверка обработчика Android `MainPhp.mainPhpFight(...)`.
   - **Обоснование:** подтвердить, почему при `fightEnded` не формируется финишный запрос.
   - **Результат:** ветка завершения требует `AppVars.FightLink` или fallback `extractFightFinishLinkFromHtml(...)`.
     - В текущем HTML этот fallback не находит `act=7/fexp`.
     - Итог: `FightLink missing` и постоянный редирект на `main.php`.

4. **Действие:** сверка `LezFight.ParseNonFight()` Android и C# эталона.
   - **Обоснование:** проверить, должен ли `FightLink` собираться для состояния `fight_ty[4]=6`.
   - **Результат:** и Android, и C# не имеют отдельной ветки `case "6"` в `ParseNonFight`.
     - Для состояния `6` `FightLink` не формируется.
     - При этом в логе грузится `fkey.js`/`FEND` (признаки капчи завершения), но Android-ветка не поднимает captcha-flow для этого формата ответа.

## Вывод

Причина незавершения боя — **логическое зацикливание в post-finish state**:

- бой распознаётся как завершённый;
- ссылка завершения (`FightLink`) отсутствует;
- сервер продолжает отдавать fight-frame со state `fight_ty[4]=6` (похоже, шаг с капчей/подтверждением завершения);
- клиент не уходит в корректный submit этого шага и вместо этого крутит fallback-редиректы `main.php -> main.php?r=...`.

## Update — 2026-03-05 (captcha-flow implementation)

1. **Действие:** Внесены изменения в `MainPhp.java` для явной классификации завершения боя через `FinishFlowDecision`.
   - **Обоснование:** Разделить и прозрачно логировать ветки `DIRECT_FINISH_LINK`, `FEND_AUTOSUBMIT_ALLOWED`, `CAPTCHA_REQUIRED`.
   - **Результат:** Добавлены маркеры `mainPhpFight finishFlow: ...` + антизацикливание по ключу `LogBoi|challengeHash` + троттлинг повторного `FEND` auto-submit.

2. **Действие:** Добавлены fingerprint-маркеры captcha-flow в `WebViewRequestInterceptor.java`.
   - **Обоснование:** Получать компактный признак серверного состояния в raw/processed ответах.
   - **Результат:** В лог добавлен маркер `[CAPTCHA_FLOW][raw|processed]` с полями `hasFEND`, `hasCodeInput`, `hasFkeyJs`, `hasCaptchaImage`, `hasFinishAct7`, `fendAction`.

3. **Действие:** Обновлен `TODO/Debug/todo_CaptchaChallengeFlow_20260305.md`.
   - **Обоснование:** Зафиксировать выполненные пункты текущего этапа.
   - **Результат:** Отмечены как выполненные блоки по `FinishFlowDecision`, лог-маркерам и антизацикливанию.

4. **Действие:** Проверка сборки.
   - **Команда:** `./gradlew :app:compileDebugJavaWithJavac`
   - **Результат:** `BUILD SUCCESSFUL`.
