# Журнал отладки 2026-02-27 23:50 — чат, отправка и системные сообщения

1) **Действие:** анализ `Logs/logcat_runtime_20260227_26.txt`.  
   **Обоснование:** понять, почему текст не отправляется и нет системных сообщений.  
   **Результат:** есть `ch/but.php` и регулярный `ch.php?show=1`, но нет следов POST/ответа на отправку; `ch_msg_v01.js` загружается.  
   **Выводы:** отправка формы `FBT` не уходит в `ch_refr`; `Chat.addMessageToChat` ранее был заглушкой.

2) **Действие:** реализован перехват отправки формы `FBT` в `ButPhp` через `AndroidBridge.chatSubmit`.  
   **Обоснование:** обойти отсутствие реальных frames/target в WebView.  
   **Результат:** добавлен JS‑хук `submit`/`form.submit` с сериализацией FormData.  
   **Выводы:** отправка должна уходить через `chatRefrWebView.postUrl`.

3) **Действие:** добавлены `chatSubmit` и `postChatRefrUrl`.  
   **Обоснование:** обеспечить POST в `ch_refr` и логирование.  
   **Результат:** `WebAppInterface.chatSubmit` вызывает `MainActivity.postChatRefrUrl`.  
   **Выводы:** ожидаем POST в логах и появление `top.add_msg` в ответе.

4) **Действие:** исправлено отображение системных сообщений через `Chat.addMessageToChat`.  
   **Обоснование:** ранее сообщения не попадали в `chatMsgWebview`.  
   **Результат:** `addMessageToChat` теперь вызывает `add_msg` в чате.  
   **Выводы:** системные сообщения из MainActivity должны отображаться.
