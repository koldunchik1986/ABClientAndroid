# Debug log 2026-02-28 15:15

1. **Действие:** Прочитан `Logs/logcat_runtime_20260227_35.txt` (фрагменты `ch.php?show=1`).  
   **Обоснование:** Проверить, как приходят клан‑сообщения в `add_msg`.  
   **Результат:** Сервер передаёт маркеры в виде `%<clan>`/`%<pair>` (не `clan:`), поэтому прежний regex `\\bclan:\\b` не срабатывает.  
   **Выводы:** Определение клан/парных сообщений нужно привязать к `%<clan>`/`%<pair>` или проставлять `%%`/`%%%` прямо в `alt`.

2. **Действие:** Обновлён `ChMsgJs.process` и логика `chatSayPrivate`.  
   **Обоснование:** Нужна стабильная схема префиксов для клик‑вставки `%clan%/%pair%`.  
   **Результат:**  
   - в `ch_msg_v01.js` проставляются `%%`/`%%%` в `alt` при наличии `%<clan>`/`%<pair>`;  
   - `to_what_who` учитывает `%<clan>`/`%<pair>` и количество `%`;  
   - `chatSayPrivate` разбирает `%%` как clan и `%%%` как pair.  
   **Выводы:** Клик по нику в клан‑сообщении должен вставлять `%clan%%<Nick>%` без зависимости от текущего текста в поле ввода.
