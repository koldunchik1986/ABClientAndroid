# debug_log_20260324_004242

## 1) Действие
- Прочитан `AGENTS.md` и подтверждены правила по отладке и TODO-документации.

### Обоснование
- По правилам проекта перед изменениями нужно опираться на регламент анализа/отладки.

### Результат
- Выбрана тактика: сначала лог-диагностика, затем точечный фикс в `app`.

### Вывод
- Изменения допускаются только в Android-коде (`app`), без правок `ABClient`.

---

## 2) Действие
- Проанализированы логи:
  - `Logs/logcat_runtime_20260324_03.txt`
  - `Logs/logcat_runtime_20260324_02.txt`
- Найдены события `MAP_NAME_SYNC_TRACE` и `ExtMap: persistLabel`.

### Обоснование
- Нужно подтвердить место рассинхрона между room-html и подтверждением позиции карты.

### Результат
- Зафиксирован deferred-путь синхронизации через `RoomManager`.
- Подтверждено, что deferred-значение имени не было жёстко привязано к `regNum`.

### Вывод
- Возможна запись имени в неверную клетку при асинхронном подтверждении позиции.

---

## 3) Действие
- Проверен код:
  - `app/src/main/java/ru/neverlands/abclient/manager/RoomManager.java`
  - `app/src/main/java/ru/neverlands/abclient/postfilter/MapAjax.java`
  - `app/src/main/java/ru/neverlands/abclient/bridge/WebAppInterface.java`
  - `app/src/main/java/ru/neverlands/abclient/utils/ExtMap.java`

### Обоснование
- Нужно пройти всю цепочку от источника имени до рендера подписи клетки.

### Результат
- Обнаружено:
  - deferred-кэш (`pendingRoomLocationName`) хранил только имя без целевого `regNum`;
  - `onMapLocationConfirmed(...)` мог применить deferred-имя к любому подтверждённому `regNum`;
  - в `resolveCellRegNumForRoomName(...)` был fallback на `currentReg` даже в `AutoMoving`, что повышало риск записи в «предыдущую» клетку.

### Вывод
- Корневая причина: слабая привязка deferred-обновления к конкретной клетке + небезопасный fallback при движении.

---

## 4) Действие
- Внесён фикс в `RoomManager.java`.

### Обоснование
- Нужно исключить применение отложенного имени к неподтверждённой клетке.

### Результат
- Добавлено поле `pendingRoomLocationTargetRegNum`.
- Deferred-кэш теперь сохраняет `serverName + targetRegNum`.
- `onMapLocationConfirmed(...)` применяет deferred-обновление только при совпадении `confirmedReg == targetReg`.
- При `AutoMoving=true` и отсутствии надёжного совпадения по имени убран fallback на `currentReg`.

### Вывод
- Вероятность ошибочного обновления подписи предыдущей клетки устранена.

---

## 5) Действие
- Выполнена проверка сборки: `./gradlew :app:compileDebugJavaWithJavac`.

### Обоснование
- Подтвердить, что изменения не ломают компиляцию.

### Результат
- `BUILD SUCCESSFUL`.
- Предупреждения только deprecation в `MainActivity` (не связаны с текущим фиксом).

### Вывод
- Фикс технически интегрирован корректно.
