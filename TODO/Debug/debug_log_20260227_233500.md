# Журнал отладки 2026-02-27 23:35 — отправка сообщений чата

1) **Действие:** анализ `Logs/logcat_runtime_20260227_25.txt`.  
   **Обоснование:** понять почему текст не отправляется.  
   **Результат:** запросы `ch.php?show=1` идут, ошибок JS в чате нет, POST в `ch/but.php` не видно; окно чата загружается.  
   **Выводы:** форма отправки, вероятно, уходит в `target="ch_refr"` и не создаёт окно (WebView onCreateWindow возвращает false).

2) **Действие:** модификация `MainActivity.onCreateWindow` для `chatButtonsWebview`.  
   **Обоснование:** направить `target="ch_refr"` в скрытый WebView.  
   **Результат:** добавлен редирект на `chatRefrWebView` при создании нового окна из `chatButtonsWebview`.  
   **Выводы:** отправка формы должна пойти в `ch_refr`, а ответ JS — обновить чат.

3) **Действие:** частичное портирование `ButPhp` (см. C#).  
   **Обоснование:** убрать вызовы `smile_open` и привести HTML к ожидаемому виду.  
   **Результат:** в Android `ButPhp` заменяет `smile_open` на `AndroidBridge.showSmiles`.  
   **Выводы:** уменьшили влияние `window.open` и улучшили совместимость.
