
### 1. План портирования FormSettingsAutoCut.resx

Файл `FormSettingsAutoCut.resx` — это XML-файл ресурсов, связанный с формой `FormSettingsAutoCut`.

### 2. Функциональность в C#

- **Назначение:** Хранение строковых ресурсов для UI формы.

### 3. Решение для портирования на Android

Этот файл **не портируется напрямую**. Android использует собственную систему ресурсов.

- **Строки:** Все текстовые значения (заголовок окна, тексты кнопок) должны быть извлечены и помещены в `res/values/strings.xml`.

### 4. План реализации

- [x] Файл не требует портирования. Использовать как источник для извлечения строковых ресурсов и их переноса в `strings.xml`.
