# Анализ `InvEntry.cs` (актуализация)

## 1. Назначение файла в C#

`ABClient/InvEntry.cs` — это ключевая модель элемента инвентаря, которая:
- парсит HTML одного предмета;
- хранит все данные для сравнения/группировки;
- умеет перестраивать HTML записи (`Build`);
- добавляет кнопки массовых действий (`AddBulkSell`, `AddBulkDelete`);
- участвует в 1:1-алгоритме объединения дублей через `CompareTo` + `CompareDolg`.

## 2. Что именно делает C# `InvEntry`

- Конструктор `InvEntry(string html)` извлекает:
  - `Name`, `WearLink`, `DropThing/DropLink/DropPrice`, `PssThing/PssLink/PssPrice`;
  - `img`, `level`, свойства предмета, долговечность (`_dolgOne/_dolgTwo`);
  - срок годности и флаг просрочки (`_expirible`, `_expired`);
  - количество action-кнопок (`_countButton`) как часть ключа сравнения.
- `CompareTo(...)` в C# сложный и чувствительный к множеству полей (не только имя).
- `CompareDolg(...)` сравнивает долговечность через числовые части `x/y`, учитывая полноту.
- `Build()`:
  - для пачки вставляет `(<N> шт.)` в заголовок;
  - для просрочки меняет палитру фона и вставляет маркер `ПРОСРОЧЕНО!`.
- `AddBulkSell()` / `AddBulkDelete()` внедряют дополнительные кнопки в HTML одной записи.

## 3. Связанный контекст

- Для группировки использован связанный эталонный файл:
  - `ABClient/PostFilter/MainPhpInv.cs`.
- `y.cs` признан опечаткой и исключён из плана работ.

## 4. Состояние Android (факт по текущему коду)

### Что уже реализовано

- [x] Есть обработка инвентаря в `MainPhp.mainPhpInv(...)`.
- [x] Есть `InvEntry`-модель в `app/src/main/java/ru/neverlands/abclient/model/InvEntry.java`.
- [x] Есть базовая группировка по `compareTo` + `compareDolg`.
- [x] Есть базовые bulk-кнопки в `model/InvEntry.addBulkSell/addBulkDelete`.
- [x] Есть профильные флаги `DoInvPack`, `DoInvSort`, `DoButtonSell`, `DoButtonDrop`.

### Что пока не 1:1

- [~] Полный парсинг C#-полей перенесён частично (ядро перенесено: expiry/drop/sell/`_countButton`/`properties`; остаются edge-case шаблоны).
- [x] `compareTo` в Android приведён к C#-подобной схеме сравнения.
- [x] `compareDolg` в Android приведён к C#-подобной логике `x/y`.
- [ ] Нет parity-поведения `DoInvPackDolg`.
- [x] `InvComparer.java` больше не заглушка.
- [ ] Есть технический долг: дублирование двух классов `InvEntry` (`model` и `postfilter`), один из них с неиспользуемыми `TODO`.

## 5. Матрица паритета (C# -> Android)

| Блок | C# | Android | Статус |
| --- | --- | --- | --- |
| Парсинг одной записи | Полный string-parsing + действия | Портирован основной каркас + fallback | `[~]` |
| Ключ сравнения | Сложный `CompareTo` | C#-подобный `compareTo` | `[x]` |
| Сравнение долговечности | `CompareDolg` по `x/y` | C#-подобный `compareDolg` | `[x]` |
| Группировка дублей | `MainPhpInv + InvEntry` | `MainPhp.mainPhpInv + model.InvEntry` | `[x]` |
| Bulk Sell/Delete | Полноценные вставки | Портированы в `model.InvEntry` | `[~]` |
| Просрочка предмета | Подсветка + метка | Портированы в `build()` | `[x]` |

## 6. План доработки (без кода, только аналитический этап)

1. [ ] Принять единый рабочий `InvEntry` и убрать дублирование класса.
2. [ ] Довести парсинг `InvEntry` до полного C#-паритета по edge-case шаблонам.
3. [x] Перенести точный `CompareTo` + `CompareDolg`.
4. [x] Перенести `Build()` для пачки и просрочки 1:1 (основной сценарий).
5. [~] Довести bulk-кнопки до C#-эквивалента (основной сценарий перенесён, нужна полировка по edge-case).
6. [ ] Прогнать валидацию на категориях инвентаря (`im`, `wca`) и обычном `go=inv`.
