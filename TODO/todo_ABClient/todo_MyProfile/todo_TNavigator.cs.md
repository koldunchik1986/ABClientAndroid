# План портирования TNavigator.cs

Файл `TNavigator.cs` определяет простую структуру для хранения настроек навигатора.

## Функциональность в C#

*   **Назначение**: Хранить настройку, разрешающую или запрещающую навигатору использовать телепорты.
*   **Реализация**: Является структурой (`struct`).
*   **Свойства**:
    *   `AllowTeleports`: `bool`.

## Проверка на существующую реализацию в Android

- **Результат:** Функциональность не реализована.

## Решение для портирования на Android

Необходимо создать POJO-класс `TNavigator.java`.

## План реализации

- [ ] **Создать файл `TNavigator.java`** в пакете `ru.neverlands.abclient.model`.

- [ ] **Определить поля класса**:
    - [ ] `public boolean allowTeleports;`

- [ ] **Создать конструктор**.

- [ ] **Интегрировать в `UserConfig.java`**:
    - [ ] Добавить новое поле: `public TNavigator navigator = new TNavigator();`.

- [ ] **Обновить `todo_MyProfile.md`**, отметив `TNavigator.cs` как проанализированный.
