# Задача: доработка UI вкладок Авто-Боя (Ротация/Группы/Останов)

## Требования

- [x] Пересортировать категории заклинаний во вкладке `Ротация`:
  1. Заклинания ударов
  2. Заклинания блоков
  3. Прочие заклинания
  4. Заклинания восст. HP
  5. Заклинания восст. MA
- [x] Убрать подписи у чекбоксов перед категориями (`абилки-удары`, `абилки-блоки`, `прочие абилки`).
- [x] Во вкладке `Группы` подсвечивать выбранную строку группы полупрозрачным фоном (кроме стрелки).
- [x] Во вкладке `Группы` выделить блок `Добавить группу` (увеличить шрифт, сделать жирным и красным).
- [x] Во вкладке `Группы` сделать фиолетовую обводку выпадающего списка выбора типа противника.
- [x] Перенести блок `Сообщение о нападении` из `Общие` во вкладку `Останов`.
- [x] Сделать `Сообщение о нападении` пер-групповым (настройка хранится в каждой группе противника).

## Реализация

- [x] Обновлены layout-файлы вкладок:
  - `tab_autoboi_rotation.xml`
  - `tab_autoboi_groups.xml`
  - `tab_autoboi_general.xml`
  - `tab_autoboi_stop.xml`
- [x] Добавлены ресурсы оформления:
  - `res/drawable/bg_autoboi_spinner_border.xml`
  - `res/values/colors.xml` (`ab_autoboi_group_selected_bg`)
- [x] Обновлён код:
  - `AutoBoiSettingsFragment.java` (перенос блока сообщений, подсветка группы, обновлённая логика вкладок)
  - `LezBotsGroup.java` (новое поле `AttackSay`)
  - `UserConfig.java` (чтение/запись `group@say`, backward compatibility)
  - `UnderAttackManager.java` (канал сообщения выбирается по подходящей группе противника)

## Примечание по совместимости

- [x] Старые профили без `group@say` мигрируются через fallback на глобальный `LezSay`.
- [x] Новые профили сохраняют канал анонса отдельно для каждой группы.
