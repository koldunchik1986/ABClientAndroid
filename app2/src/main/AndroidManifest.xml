<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".ANClientApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ANClient"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ANClient.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.ANClient.NoActionBar" />

        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:label="@string/profile_save"
            android:parentActivityName=".LoginActivity" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/action_settings"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".LogsActivity"
            android:exported="false"
            android:label="Логи"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ContactsActivity"
            android:exported="false"
            android:label="Контакты"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ClansActivity"
            android:exported="false"
            android:label="Кланы"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ANClient.NoActionBar" />

        <activity
            android:name=".KaznaActivity"
            android:exported="false"
            android:label="Казна"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ANClient.NoActionBar" />

        <activity
            android:name=".PinfoActivity"
            android:exported="false"
            android:label="Инфо"
            android:parentActivityName=".MainActivity" />

        <service
            android:name=".service.AutoModeForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
