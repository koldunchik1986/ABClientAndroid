<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/app_bar_main">

    <!-- Основная вкладка (вкладка 1): main.php + чат -->
    <LinearLayout
        android:id="@+id/tab_main_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:visibility="visible">

        <!-- Панель быстрых кнопок (сверху основной вкладки) -->
        <include
            android:id="@+id/quick_buttons_bar"
            layout="@layout/quick_buttons_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <WebView
            android:id="@+id/webView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <LinearLayout
            android:id="@+id/chat_container"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="0.4"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:orientation="horizontal">

                <WebView
                    android:id="@+id/chat_msg_webview"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="2" />

                <WebView
                    android:id="@+id/chat_users_webview"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1" />

            </LinearLayout>

            <WebView
                android:id="@+id/chat_buttons_webview"
                android:layout_width="match_parent"
                android:layout_height="40dp" />

        </LinearLayout>

        <include
            android:id="@+id/status_bar"
            layout="@layout/status_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Панель быстрых кнопок (снизу) -->
        <include
            android:id="@+id/quick_buttons_bar_bottom"
            layout="@layout/quick_buttons_bar_bottom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <!-- Контейнер для вспомогательных вкладок (вкладки 2+) -->
    <FrameLayout
        android:id="@+id/tab_secondary_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

</FrameLayout>
