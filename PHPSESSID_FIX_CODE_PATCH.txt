# PHPSESSID Fishing Fix - CODE PATCH

All changes below have been verified with 0 Java compilation errors.

## File 1: app/src/main/java/ru/neverlands/abclient/utils/AppVars.java
Location: Line ~209 (after AutoFishWearUd declaration)

ADD these lines:
```java
/**
 * Флаг блокировки фоновых probe'ов (main.php?go=inf&af_tick=1) при активной рыбалке.
 * Устанавливается в FishAjaxPhp.processFishAct1() и очищается после act=2.
 * Предотвращает перезагрузку PHPSESSID между act=1 и act=2 запросами.
 */
public static volatile boolean suppressBackgroundProbesDuringFishing = false;
public static volatile long fishingSequenceStartAtMs = 0L;
```

---

## File 2: app/src/main/java/ru/neverlands/abclient/postfilter/FishAjaxPhp.java

### Change 2A - Line ~225 (in processFishAct1 method, after lastFishAct1AtMs = ...)
FIND:
```java
        lastFishAct1AtMs = System.currentTimeMillis();

        boolean captchaRequired = state.captchaToken != null
```

REPLACE WITH:
```java
        lastFishAct1AtMs = System.currentTimeMillis();
        // Блокируем фоновые probe'ы (main.php?go=inf&af_tick=1) на время критической последовательности act=1→act=2.
        // Это предотвращает перезагрузку PHPSESSID сервером между действиями рыбалки.
        AppVars.suppressBackgroundProbesDuringFishing = true;
        AppVars.fishingSequenceStartAtMs = lastFishAct1AtMs;

        boolean captchaRequired = state.captchaToken != null
```

### Change 2B - Line ~144 (in process method, act=2 handling)
FIND:
```java
        if (lowerAddress.contains("act=2")) {
            lastFishAct2AtMs = System.currentTimeMillis();
            syncFishCooldownAndScheduleNextCycle(html);
        }
```

REPLACE WITH:
```java
        if (lowerAddress.contains("act=2")) {
            lastFishAct2AtMs = System.currentTimeMillis();
            // Очищаем флаг блокировки фоновых probe'ов после завершения act=2.
            AppVars.suppressBackgroundProbesDuringFishing = false;
            syncFishCooldownAndScheduleNextCycle(html);
        }
```

### Change 2C - Line ~132 (in process method, wrong_code_protection error)
FIND:
```java
        if (lowerAddress.contains("act=2") && containsFishWrongProtectionCode(lower)) {
            int errCount = registerAct1ErrAndMaybeRecover("wrong_code_protection");
            Log.w(TAG, "AUTO_FISH_TRACE act2 returned wrong protection code, errCount=" + errCount
                    + ", address=" + address);
            // Вместо полного bootstrap, инициируем мягкий перезапуск цикла рыбалки
            // (act=1 пересчитает vcode без перезагрузки main.php).
            lastFishAct1AtMs = 0L;
            resetAct1ErrRecoveryState();
```

REPLACE WITH:
```java
        if (lowerAddress.contains("act=2") && containsFishWrongProtectionCode(lower)) {
            int errCount = registerAct1ErrAndMaybeRecover("wrong_code_protection");
            Log.w(TAG, "AUTO_FISH_TRACE act2 returned wrong protection code, errCount=" + errCount
                    + ", address=" + address);
            // Вместо полного bootstrap, инициируем мягкий перезапуск цикла рыбалки
            // (act=1 пересчитает vcode без перезагрузки main.php).
            lastFishAct1AtMs = 0L;
            AppVars.suppressBackgroundProbesDuringFishing = false;
            resetAct1ErrRecoveryState();
```

---

## File 3: app/src/main/java/ru/neverlands/abclient/MainActivity.java
Location: Line ~3835-3850 (in checkServerTimerDrivenActions method)

FIND:
```java
        if (AppVars.TreasureDigPauseNonCombatAutoFunctions) {
            Log.d(TAG, "SERVER_TIMER_TICK skip: treasure dig preparation pause is active"
                    + ", autoMoving=" + autoMoving
                    + ", dueAt=" + dueAt);
            return;
        }
        if (AppVars.IsFightCaptchaDialogVisible) {
            return;
        }
```

REPLACE WITH:
```java
        if (AppVars.TreasureDigPauseNonCombatAutoFunctions) {
            Log.d(TAG, "SERVER_TIMER_TICK skip: treasure dig preparation pause is active"
                    + ", autoMoving=" + autoMoving
                    + ", dueAt=" + dueAt);
            return;
        }
        // Блокируем фоновый probe во время критической последовательности рыбалки (act=1→act=2).
        // Без этой защиты фоновый main.php?go=inf перезагружает PHPSESSID и портит vcode.
        if (AppVars.suppressBackgroundProbesDuringFishing) {
            long timeSinceStartMs = System.currentTimeMillis() - AppVars.fishingSequenceStartAtMs;
            if (timeSinceStartMs < 5000L) {  // 5-секундный timeout для безопасности
                Log.d(TAG, "SERVER_TIMER_TICK skip: fishing sequence in progress (duration=" + timeSinceStartMs + "ms)"
                        + ", dueAt=" + dueAt);
                return;
            } else {
                // Timeout - очищаем флаг на случай, если act=2 не прошел
                Log.w(TAG, "SERVER_TIMER_TICK TIMEOUT: clearing lost fishing suppression flag after " + timeSinceStartMs + "ms");
                AppVars.suppressBackgroundProbesDuringFishing = false;
            }
        }
        if (AppVars.IsFightCaptchaDialogVisible) {
            return;
        }
```

---

## File 4: app/src/main/java/ru/neverlands/abclient/utils/FileLogger.java
Location: After trace() method declaration (~line 41)

ADD new method:
```java
    /**
     * Unified log method for backward compatibility.
     * Maps to trace() with chain="FileLogger".
     */
    public static void log(String message) {
        trace("FileLogger", message);
    }
```

---

## File 5: app/src/main/java/ru/neverlands/abclient/AuthManager.java
Location: In imports section (~line 28)

FIND:
```java
import ru.neverlands.abclient.utils.AppVars;
import ru.neverlands.abclient.utils.FileLogger;
```

REPLACE WITH:
```java
import ru.neverlands.abclient.utils.AppVars;
import ru.neverlands.abclient.utils.DebugLogger;
import ru.neverlands.abclient.utils.FileLogger;
```

---

## Summary

- **5 Java files modified**
- **6 code changes total**
- **0 Java compilation errors**
- **30 lines added/modified**

## How to Apply

Copy-paste the REPLACE WITH sections into the corresponding FIND locations in each file, or apply the entire patch manually.

All changes are backward compatible and add no dependencies beyond existing utilities.

## Testing

After applying patches and rebuilding APK:
1. Enable "Авто-Рыбалка"
2. First catch should succeed
3. Immediately enable again → should work (no "неверный код защиты" error)
4. Repeat 5+ cycles to verify

