# Задача: Исправление отображения списка игроков в комнате (Завершено)

- [x] **Проблема:** Список игроков не отображался в Android-приложении.
- [x] **Решение:** Исправлена сетевая ошибка (отложенный запуск опроса) и JS-мост (заглушки для `window.external`).

# Задача: Исправление быстрых действий (Fast Actions)

- [x] **Проблема:** Быстрые действия (нападения, свитки) не срабатывали на вкладке "Ваш персонаж" (`get_id=56&go=inf`), так как ожидали список предметов в HTML.
- [x] **Анализ:** Требуется автоматический переход на вкладку инвентаря (`go=inv`) или под-вкладки (`wca=28`, `wca=27`) с использованием актуального `vcode` из HTML.
- [x] **Решение:**
    - Обновлен `FastActionManager.java`: добавлен интеллектуальный поиск ссылок в HTML и автоматический редирект (JS-редирект через `location='...'`).
    - Обновлен `reloadMainFrame` для поддержки `vcode` при формировании начального URL.
- [ ] **Статус:** Ожидает тестирования в v1.0.8.

# Задача: Автоматическое лечение (AutoCure)

- [ ] **Описание:** Портирование логики из `MainPhpAutoCure.cs` для автоматического использования свитков/способностей лечения.
- [ ] **Анализ:** Требуется перехват `main.php` при низком уровне HP/MP и генерация форм использования лечения.
- [ ] **Статус:** В планах.
